apiVersion: registry.solo.io/v1alpha1
kind: Agent
metadata:
  name: cost-analyzer-agent
  namespace: devai
  visibility: public
  labels:
    devai.io/source: devai
    devai.io/category: sre
    devai.io/skill: cost-analyzer
spec:
  skill: cost-analyzer
  promptRef: cost-analyzer-prompt-v1
  llm:
    provider: openai
    model: gpt-4.1
    temperature: null
    maxTokens: null
  limits:
    maxTurns: 25
    timeoutSeconds: 900
  runtime:
    kind: python_class
    pythonClass: devai.sre.agents.cost_analyzer.CostAnalyzerAgent
  riskLevel: low
  roleColor: admin
