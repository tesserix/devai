apiVersion: registry.solo.io/v1alpha1
kind: Tool
metadata:
  name: sandbox-browser-get-content
  namespace: devai
  visibility: public
  labels:
    devai.io/source: devai
    mcp.devai.io/server: sandbox
    devai.io/domain: sandbox
    devai.io/tier: core
    devai.io/risk-level: low
  annotations:
    mcp.devai.io/wire-name: browser_get_content
spec:
  displayName: Browser Get Content
  description: Read the current page HTML or an element's visible text.
  version: '1'
  server: sandbox
  inputSchema:
    type: object
    properties:
      selector:
        type: string
    required: []
  tags: [sandbox, mcp-tool, browser]
