apiVersion: registry.solo.io/v1alpha1
kind: Tool
metadata:
  name: gitops-kargo-list-projects
  namespace: devai
  visibility: public
  labels:
    devai.io/source: devai
    mcp.devai.io/server: gitops-mcp
    devai.io/domain: gitops
    devai.io/tier: core
    devai.io/risk-level: low
  annotations:
    mcp.devai.io/wire-name: kargo_list_projects
spec:
  displayName: Kargo List Projects
  description: List Kargo projects (delivery pipelines).
  version: '1'
  server: gitops-mcp
  inputSchema:
    type: object
    properties:
      cluster:
        type: string
        description: "Optional \u2014 the name of one of YOUR connected Kubernetes clusters (Settings\
          \ \u2192 Kubernetes Cluster). Omit to use the platform cluster."
      kargo:
        type: string
        description: "Optional \u2014 the name of one of YOUR connected Kargo control planes (Settings\
          \ \u2192 Kargo, API mode). Targets that Kargo's API directly."
  tags:
  - gitops
  - mcp-tool
