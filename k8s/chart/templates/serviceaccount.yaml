{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "devai.serviceAccountName" . }}
  labels:
    {{- include "devai.labels" . | nindent 4 }}
{{- end }}
